# import twitchio
# import asyncio
# import os
# from computer_vision import computer_vision


#     def handle_prediction(prediction):
#         print(prediction.output)

#     prediction.add_done_callback(handle_prediction)

# asyncio.get_event_loop().run_until_complete(computer_vision.minigpt(prompt, image_url))